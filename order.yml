server:
  port: 9098

spring:
  application:
    name: order
  datasource:
    druid:
      url: jdbc:mysql://121.4.152.17:3306/simpleproduct_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: root
      password: 123

jpa:
  hibernate:
    ddl-auto: update
  show-sql: true

eureka:
  client:
    service-url: 
      defaultZone: http://localhost:8762/eureka/
